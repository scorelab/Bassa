apiVersion: v1
kind: Service
metadata:
  name: {{ include "bassa-api.fullname" . }}
  labels:
    {{- include "bassa-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bassa-api.selectorLabels" . | nindent 4 }}
