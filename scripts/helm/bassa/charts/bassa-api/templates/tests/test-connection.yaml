apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bassa-api.fullname" . }}-test-connection"
  labels:
    {{- include "bassa-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bassa-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
