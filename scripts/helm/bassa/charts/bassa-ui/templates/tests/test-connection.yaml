apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bassa-ui.fullname" . }}-test-connection"
  labels:
    {{- include "bassa-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bassa-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
