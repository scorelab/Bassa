apiVersion: v1
kind: Service
metadata:
  name: {{ include "bassa-ui.fullname" . }}
  labels:
    {{- include "bassa-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bassa-ui.selectorLabels" . | nindent 4 }}
