apiVersion: v1
kind: Service
metadata:
  name: {{ include "bassa-db.fullname" . }}
  labels:
    {{- include "bassa-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bassa-db.selectorLabels" . | nindent 4 }}
