apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bassa-db.fullname" . }}-test-connection"
  labels:
    {{- include "bassa-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bassa-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
